pipeline {
    agent any
environment { 
        mvnHome = 'Maven'
    }
    stages {
        stage('Build') {
            steps {
            
                sh 'mvnHome/bin/mvn clean install'
            }
        }
    }
}

pipeline {
    agent any
environment { 
        mvnHome = 'Maven'
    }
    stages {
        stage('Build') {
            steps {
            
                sh 'mvn clean install'
            }
        }
    }
}
